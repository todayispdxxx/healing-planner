# GitHub Pages 部署指南

本项目已配置 GitHub Actions 自动部署，推送代码到 GitHub 后会自动构建并部署到 GitHub Pages。

## 部署步骤

### 1. 在 GitHub 创建仓库

登录 GitHub，创建一个新仓库，命名为 `healing-planner`（或你喜欢的名字）。
- 可选：Public（公开，GitHub Pages 免费）或 Private（私有，需要 GitHub Pro 才能用 Pages）
- **不要**勾选 "Add a README" / "Add .gitignore" / "Choose a license"（项目已有这些文件）

### 2. 初始化并推送代码

在项目根目录 `healing-planner/` 下执行：

```bash
# 初始化 git 仓库
git init

# 添加所有文件（node_modules 已被 .gitignore 忽略）
git add .

# 首次提交
git commit -m "feat: 治愈系智能计划管理应用初始版本"

# 设置主分支名
git branch -M main

# 关联远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/healing-planner.git

# 推送
git push -u origin main
```

### 3. 启用 GitHub Pages

1. 进入仓库 **Settings** → **Pages**
2. **Source** 选择 **GitHub Actions**
3. 推送代码后，`.github/workflows/deploy.yml` 会自动触发构建

### 4. 查看部署结果

- 推送后，在仓库 **Actions** 标签页可看到 "Deploy to GitHub Pages" 工作流
- 构建成功后（约 1-2 分钟），访问地址为：
  ```
  https://你的用户名.github.io/healing-planner/
  ```
- 如果仓库名为 `你的用户名.github.io`，则访问 `https://你的用户名.github.io/`

### 5. 更新论坛文章中的 Demo 链接

部署成功后，将在线 Demo 地址填入 `华为云论坛发表文章.md` 中所有 `[待部署后补充链接]` 的位置。

## 技术说明

- **vite.config.js** 中 `base: './'` 使用相对路径，配合 Hash 路由（`createWebHashHistory`），兼容 GitHub Pages 子路径部署
- **GitHub Actions** 工作流（`.github/workflows/deploy.yml`）会在每次推送到 `main` 分支时自动构建并部署
- 构建产物在 `dist/` 目录，通过 `actions/upload-pages-artifact` 上传到 GitHub Pages

## 常见问题

### Q: 部署后页面空白？

检查浏览器控制台是否有 404 错误。确认 `vite.config.js` 的 `base` 配置正确。当前使用 `base: './'` + Hash 路由，应该兼容子路径部署。

### Q: 构建失败？

查看 Actions 标签页的构建日志。常见原因：
- Node.js 版本不匹配：工作流使用 Node 20，确保 `package.json` 的 engines 兼容
- 依赖安装失败：检查 `package-lock.json` 是否一致

### Q: 如何更新部署？

每次 `git push` 到 `main` 分支都会自动触发重新部署。也可以在 Actions 页面手动触发 `workflow_dispatch`。

## Electron 桌面版本

GitHub Pages 部署的是 Web 版本（不含 Electron 桌面特性）。如需构建桌面安装包：

```bash
npm run electron:build
```

构建产物在 `dist-electron/` 目录，生成 NSIS 安装包（`.exe`）。