# Electron + Vue 3 实战：从零构建治愈系智能计划管理桌面应用

> 本文完整记录一款面向学生和上班族的 PC 端治愈系智能计划管理应用的构建过程。基于 Electron 33 + Vue 3 + Vite 6 + Pinia 技术栈，实现自然语言任务解析、语音输入、AI 智能解析、桌面便利贴、可拖动桌宠等核心功能，并打包为 Windows 桌面应用。
>
> **在线体验**：[GitHub Pages 在线 Demo](待部署后补充链接)
> **技术栈**：Electron 33 / Vue 3.5 / Vite 6 / Pinia 2.2 / Vue Router 4 / 纯手写 CSS

## 一、项目背景与目标

在快节奏的学习和工作环境中，学生和上班族常常面临任务繁杂、时间管理混乱的问题。传统的待办事项应用界面生硬、功能单一，缺乏人性化的交互体验。

本项目旨在构建一款**治愈系智能计划管理桌面应用**，通过以下手段解决痛点：

- **自然语言解析**：一句话输入"明天上午十点交报告，下午三点开组会"，自动拆分为多个任务并提取时间、类别、优先级
- **语音输入**：支持在线识别和录音转写双模式，解放双手
- **AI 智能解析**：支持接入 OpenAI/DeepSeek/智谱/Moonshot/通义千问等大模型，内置 Mock 模式免 API 测试
- **治愈风格 UI**：奶油白底色、柔和配色、圆润卡片、桌宠陪伴，让任务管理不再枯燥

### 核心功能一览

| 功能模块 | 说明 |
|----------|------|
| 智能任务输入 | 自然语言输入，自动拆分多任务，提取时间/类别/优先级/提醒时间 |
| 任务管理 | 今日/本周/全部视图，按类别、优先级筛选，增删改查 |
| 温馨提醒 | 应用内 Toast + Electron 原生系统通知双通道，温和文案 |
| 桌面便利贴 | 任务生成便利贴，7 种类别对应 7 种颜色 |
| 桌宠陪伴 | 可拖动桌宠，点击互动（眨眼/飘心/随机文案），4 种心情状态 |
| 语音输入 | Web Speech API 在线识别 + MediaRecorder 录音转写 |
| 桌面应用 | 无边框窗口、系统托盘、窗口状态记忆、开机自启、NSIS 安装包 |

## 二、技术选型与理由

| 技术 | 选型 | 理由 |
|------|------|------|
| 桌面框架 | Electron 33 | 跨平台 PC 桌面应用，支持系统通知、托盘、原生能力 |
| 前端框架 | Vue 3 + Vite 6 | Composition API + `<script setup>`，开发体验好，构建快 |
| 状态管理 | Pinia 2.2 | Vue 3 官方推荐，轻量灵活 |
| 路由 | Vue Router 4.4 | Hash 模式（适配 Electron），懒加载 |
| 本地存储 | localStorage | 軻量持久化，无需数据库服务 |
| 语音识别 | Web Speech API + MediaRecorder | 在线识别零依赖，录音转写支持多家 STT API |
| AI 接口 | OpenAI 兼容 + Mock | 支持 6 家提供商 + 自定义，内置模拟测试 |
| UI 方案 | 纯手写 CSS | CSS 变量驱动治愈主题，零第三方 UI 库依赖 |
| 打包 | electron-builder + NSIS | Windows 安装包，支持自定义安装目录 |

> **选型亮点**：整个项目零第三方 UI 库依赖，纯手写 CSS 通过 CSS 变量驱动完整的设计系统；AI 接口采用 OpenAI 兼容协议，一套代码兼容 6 家主流大模型提供商。

## 三、项目架构设计

应用采用**分层架构**，各层职责清晰：

```
┌──────────────────────────────────────────────┐
│              Views（页面层）                    │  5 个页面视图，路由分发
├──────────────────────────────────────────────┤
│           Components（组件层）                 │  可复用 UI 组件
├──────────────────────────────────────────────┤
│            Stores（状态层）                    │  Pinia 状态管理，5 个 Store
├──────────────────────────────────────────────┤
│           Services（服务层）                   │  业务逻辑与外部接口
├──────────────────────────────────────────────┤
│           Storage（持久层）                    │  localStorage 数据存储
└──────────────────────────────────────────────┘
         ↕ IPC 通信
┌──────────────────────────────────────────────┐
│         Electron 主进程                        │  窗口管理 / 托盘 / 通知 / 自启
└──────────────────────────────────────────────┘
```

### 目录结构

```
healing-planner/
├── electron/                     # Electron 主进程
│   ├── main.js                   # 窗口创建、系统托盘、IPC、原生通知、开机自启
│   └── preload.js                # contextBridge 安全桥接
├── src/
│   ├── assets/styles/main.css    # 全局样式（CSS 变量、治愈主题、动画）
│   ├── components/
│   │   ├── layout/               # TitleBar / Sidebar
│   │   ├── pet/DesktopPet.vue    # 桌宠（拖拽/互动/心情/进度环）
│   │   ├── reminder/             # 提醒 Toast
│   │   ├── sticky/               # 便利贴卡片
│   │   └── task/                 # 任务卡片/编辑弹窗/快速添加
│   ├── router/index.js           # Hash 模式路由，5 页面懒加载
│   ├── services/                 # 业务逻辑层
│   │   ├── storage.js            # localStorage 持久化
│   │   ├── taskParser.js         # 自然语言任务解析引擎
│   │   ├── mockAI.js             # AI 模拟解析
│   │   ├── voiceInput.js         # Web Speech API 在线语音识别
│   │   └── voiceRecorder.js      # MediaRecorder 录音 + STT 转写
│   ├── stores/                   # Pinia 状态管理（5 个 Store）
│   └── views/                    # 5 个页面视图
├── package.json
└── vite.config.js
```

> **关键设计**：Services 层是核心，`taskParser.js` 负责规则解析，`mockAI.js` 负责模拟 AI 解析，两者可无缝切换，方便后续接入真实大模型。Electron 主进程通过 `preload.js` 的 `contextBridge` 安全暴露 API，渲染进程通过 `window.electronAPI` 调用。

## 四、核心功能实现详解

### 4.1 Electron 桌面应用壳：无边框窗口与系统托盘

Electron 主进程负责窗口管理、系统托盘、原生通知、开机自启等桌面能力，是应用从"网页"升级为"桌面应用"的核心。

**无边框窗口与状态记忆**：

```javascript
// 窗口状态持久化 —— 记住用户上次的位置和大小
function loadWindowState() {
  try {
    const data = fs.readFileSync(windowStatePath, 'utf-8')
    return JSON.parse(data)
  } catch { return null }
}

const mainWindow = new BrowserWindow({
  frame: false,                    // 无边框，使用自定义 TitleBar
  show: false,                     // 防白屏：先隐藏，ready-to-show 后再显示
  backgroundColor: '#FFF9F0',      // 与 CSS 变量一致的暖色背景
  minWidth: 900, minHeight: 600,
  width: savedState?.width || 1200,
  height: savedState?.height || 800,
  x: savedState?.x, y: savedState?.y,
  webPreferences: {
    preload: path.join(__dirname, 'preload.js'),
    contextIsolation: true,        // 安全隔离
    nodeIntegration: false
  }
})
```

**系统托盘**：

```javascript
const tray = new Tray(nativeImage.createFromDataURL(ICON_BASE64))

tray.on('double-click', () => mainWindow.show())

const contextMenu = Menu.buildFromTemplate([
  { label: '打开治愈计划', click: () => mainWindow.show() },
  { label: '快速添加任务', click: () => {
    mainWindow.show()
    mainWindow.webContents.send('navigate', '/input')
  }},
  { type: 'separator' },
  { label: '退出', click: () => { app.quit() } }
])
tray.setContextMenu(contextMenu)
```

**IPC 安全桥接**（preload.js）：

```javascript
contextBridge.exposeInMainWorld('electronAPI', {
  minimizeWindow: () => ipcRenderer.send('window-minimize'),
  maximizeWindow: () => ipcRenderer.send('window-maximize'),
  closeWindow: () => ipcRenderer.send('window-close'),
  showNotification: (title, body) => ipcRenderer.send('show-notification', title, body),
  isMaximized: () => ipcRenderer.invoke('window-is-maximized'),
  setAutoLaunch: (enabled) => ipcRenderer.invoke('set-auto-launch', enabled),
  onNavigate: (callback) => ipcRenderer.on('navigate', (_, path) => callback(path))
})
```

> **设计要点**：
> - `frame: false` 去掉原生标题栏，前端使用自定义 `TitleBar.vue` 替代
> - `show: false` + `ready-to-show` 事件避免窗口白屏闪烁
> - `contextIsolation: true` + `contextBridge` 确保渲染进程无法直接访问 Node.js API
> - 最小化和关闭都隐藏到托盘而非退出，符合桌面应用习惯

### 4.2 自然语言任务解析引擎

用户输入一段自然语言文本，系统自动拆分为多个任务，提取标题、时间、类别、优先级等字段。

**时间表达式匹配**（共 25 种模式，按优先级从具体到模糊排列）：

```javascript
const TIME_PATTERNS = [
  { pattern: /明天上午(\d+)[点时:：](\d+)?分?/, handler: (m, now) => getNextDayTime(now, parseInt(m[1]), m[2] ? parseInt(m[2]) : 0) },
  { pattern: /明天下午(\d+)[点时:：](\d+)?分?/, handler: (m, now) => getNextDayTime(now, parseInt(m[1]) + 12, m[2] ? parseInt(m[2]) : 0) },
  { pattern: /明天上午/, handler: (m, now) => getNextDayTime(now, 10, 0) },
  { pattern: /明天/, handler: (m, now) => getNextDayTime(now, 9, 0) },
  { pattern: /周([一二三四五六日天])前/, handler: (m, now) => getThisWeekdayTime(now, m[1]) },
  { pattern: /后天/, handler: (m, now) => getRelativeDayTime(now, 2) },
  { pattern: /大后天/, handler: (m, now) => getRelativeDayTime(now, 3) },
  { pattern: /(\d+)天后/, handler: (m, now) => getRelativeDayTime(now, parseInt(m[1])) },
  // ... 更多规则
]
```

**类别检测**（7 种类别关键词映射）：

```javascript
const CATEGORY_KEYWORDS = {
  '学习': ['学习', '复习', '背', '英语', '单词', '作业', '考试', '论文', '预习'],
  '工作': ['报告', 'PPT', '代码', '提交', '项目', '需求', '上线', '部署', '评审'],
  '会议': ['会议', '开会', '组会', '讨论', '汇报', '面试', '答辩'],
  '生活': ['吃饭', '运动', '健身', '打扫', '洗衣', '买菜', '做饭', '休息'],
  '购物': ['买', '购物', '下单', '取快递', '淘宝', '京东'],
  '长期计划': ['计划', '目标', '习惯', '坚持', '每天', '每周'],
  '其他': []  // 兜底类别
}
```

**提醒时间自动计算**（根据类别智能调整提前量）：

```javascript
function calculateReminderTime(deadline, category) {
  if (!deadline) return null
  const d = new Date(deadline)
  if (category === '会议') return new Date(d.getTime() - 30 * 60 * 1000)   // 会议提前 30 分钟
  if (['工作', '学习'].includes(category)) return new Date(d.getTime() - 24 * 60 * 60 * 1000) // 提前 1 天
  return new Date(d.getTime() - 60 * 60 * 1000) // 其他提前 1 小时
}
```

**解析效果示例**：

输入：`"明天上午十点交软件工程报告，下午三点开组会，周五前把PPT做完，晚上背英语单词"`

| # | 标题 | 时间 | 类别 | 优先级 | 提醒时间 |
|---|------|------|------|--------|----------|
| 1 | 交软件工程报告 | 明天 10:00 | 工作 | 中 | 明天 09:00 |
| 2 | 开组会 | 明天 15:00 | 会议 | 中 | 明天 14:30 |
| 3 | 把PPT做完 | 周五 18:00 | 工作 | 中 | 周四 18:00 |
| 4 | 背英语单词 | 今天 20:00 | 学习 | 中 | 今天 19:00 |

> **设计要点**：时间规则按**从具体到模糊**的优先级排列，先匹配"明天上午十点"再匹配"明天上午"最后匹配"明天"。`smartParse()` 是统一入口，后续可无缝替换为大模型 API 调用。

### 4.3 语音输入双模式

支持两种语音输入模式——在线识别（Web Speech API）和录音转写（MediaRecorder + STT API），适应不同网络环境。

**在线语音识别**：

```javascript
class VoiceInputService {
  constructor() {
    const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition
    if (SpeechRecognition) {
      this.recognition = new SpeechRecognition()
      this.recognition.lang = 'zh-CN'        // 中文识别
      this.recognition.continuous = true       // 连续识别模式
      this.recognition.interimResults = true   // 返回中间结果
      this.supported = true
    }
  }

  start(onResult, onEnd, onError) {
    this.recognition.onresult = (event) => {
      let final = '', interim = ''
      for (let i = event.resultIndex; i < event.results.length; i++) {
        if (event.results[i].isFinal) {
          final += event.results[i][0].transcript
        } else {
          interim += event.results[i][0].transcript
        }
      }
      onResult?.({ final, interim, all: final + interim })
    }
    this.recognition.start()
  }
}
```

**录音转写**（MediaRecorder + STT API）：

```javascript
class VoiceRecorderService {
  async startRecording() {
    const stream = await navigator.mediaDevices.getUserMedia({ audio: true })
    // 自动检测支持的 MIME 类型：webm/opus > webm > ogg > mp4
    const mimeType = ['audio/webm;codecs=opus', 'audio/webm', 'audio/ogg', 'audio/mp4']
      .find(type => MediaRecorder.isTypeSupported(type))
    this.mediaRecorder = new MediaRecorder(stream, { mimeType })
    this.chunks = []
    this.mediaRecorder.ondataavailable = (e) => this.chunks.push(e.data)
    this.mediaRecorder.start()
  }

  async stopAndTranscribe() {
    return new Promise((resolve) => {
      this.mediaRecorder.onstop = async () => {
        const blob = new Blob(this.chunks, { type: this.mediaRecorder.mimeType })
        const text = await sttService.transcribeWithAPI(blob)
        resolve(text)
      }
      this.mediaRecorder.stop()
    })
  }
}
```

> **设计要点**：双模式设计让在线识别无需额外配置即可使用，录音转写则支持 Whisper/DeepSeek/百度/阿里等 STT 服务。MIME 类型自动检测，优先使用 `webm/opus` 高质量编码。

### 4.4 AI 智能解析与 Mock 模式

支持接入 OpenAI 兼容 API 进行智能解析，同时内置 Mock 模式供无 API 场景测试。

**Mock 解析**（模拟网络延迟，体验接近真实 API）：

```javascript
function generateMockResponse(text) {
  const segments = text.split(/[，,；;。\n]/).filter(s => s.trim())
  const tasks = segments.map(seg => {
    if (/学习|复习|背|英语|单词/.test(seg)) category = '学习'
    else if (/报告|PPT|代码|提交/.test(seg)) category = '工作'
    else if (/会议|开会|组会/.test(seg)) category = '会议'
    if (/紧急|重要/.test(seg)) priority = '高'
    return { title, category, priority, deadline, ... }
  })
  return { tasks }
}

export function mockAIParse(text) {
  return new Promise((resolve) => {
    setTimeout(() => resolve(generateMockResponse(text)), 800 + Math.random() * 600)
  })
}
```

**支持的 AI 提供商**：

| 提供商 | API 格式 | 说明 |
|--------|----------|------|
| 模拟测试 | Mock | 内置模拟，无需 API Key |
| OpenAI | OpenAI 兼容 | 官方及兼容接口 |
| DeepSeek | OpenAI 兼容 | 国产大模型 |
| 智谱 AI | OpenAI 兼容 | GLM 系列 |
| Moonshot | OpenAI 兼容 | Kimi 系列 |
| 通义千问 | OpenAI 兼容 | 阿里云系列 |
| 自定义 | OpenAI 兼容 | 任意兼容接口 |

> **设计要点**：Mock 模式模拟了 800~1400ms 的网络延迟，体验接近真实 API。在智能输入页，当用户未配置 API 或选择了"模拟测试"时，自动使用 Mock，零门槛体验完整 AI 解析流程。

### 4.5 可拖动桌宠：拖动、互动与心情系统

桌宠可自由拖动到屏幕任意位置，点击触发互动动画和治愈文案，4 种心情状态随任务进度变化。

**拖动与点击交互**：

```javascript
function startDrag(e) {
  if (e.button !== 0) return        // 仅左键
  isDragging.value = true
  hasMoved = false                   // 区分点击和拖动
  dragOffsetX = e.clientX - posX.value
  dragOffsetY = e.clientY - posY.value
}

function onDrag(e) {
  if (!isDragging.value) return
  hasMoved = true
  posX.value = e.clientX - dragOffsetX
  posY.value = e.clientY - dragOffsetY
}

function stopDrag() {
  isDragging.value = false
  // 边界吸附：防止拖出可视区域
  posX.value = Math.max(0, Math.min(posX.value, window.innerWidth - 100))
  posY.value = Math.max(0, Math.min(posY.value, window.innerHeight - 160))
}

function handlePetClick() {
  if (hasMoved) return               // 拖动过则不触发点击
  isWinking.value = true             // 眨眼动画
  setTimeout(() => { isWinking.value = false }, 400)
  showHearts.value = true            // 飘心动画
  setTimeout(() => { showHearts.value = false }, 900)
  petStore.currentMessage = randomMessage()
}
```

**4 种心情状态**（随任务进度自动切换）：

```javascript
const MOOD_MAP = {
  happy:    { color: '#7DB88A', label: '开心' },    // 默认状态
  excited:  { color: '#F0B87A', label: '兴奋' },    // 完成任务时
  worried:  { color: '#89C4E1', label: '担心' },    // 任务过多时
  sad:      { color: '#89C4E1', label: '难过' }     // 任务逾期时
}
```

> **设计要点**：通过 `hasMoved` 标记区分拖动和点击，避免拖动结束触发点击事件。`document` 级别的 `mousemove`/`mouseup` 监听确保拖出组件范围仍能正常工作。互动文案 13 条，涵盖摸头、鼓励、闲聊等场景。

### 4.6 治愈风格 UI 设计系统

基于 CSS 变量构建完整的治愈风格设计系统，全局统一配色、圆角、阴影。

**色彩体系**：

```css
:root {
  /* 背景色系 —— 温暖奶油白到浅杏的层次 */
  --bg-primary: #FFF9F0;       /* 主背景（奶油白） */
  --bg-secondary: #FFF5E8;     /* 侧边栏/次要背景 */
  --bg-card: #FFFFFF;          /* 卡片背景 */

  /* 文字色系 —— 暖棕色系，高对比度但不刺眼 */
  --text-primary: #5D4E37;     /* 主文字（暖棕） */
  --text-secondary: #9E8E78;   /* 次要文字 */

  /* 主色调 —— 温暖绿 */
  --accent: #7DB88A;

  /* 辅助色 —— 柔和的治愈色系 */
  --accent-green: #8BC99A;
  --accent-blue: #89C4E1;
  --accent-pink: #F2A7B3;
  --accent-yellow: #F5D98A;
  --accent-orange: #F0B87A;
}
```

**7 种任务类别色**（浅底深字原则）：

```css
:root {
  --color-study: #E8F5E9;       --color-study-text: #4CAF50;     /* 学习 - 浅绿 */
  --color-work: #E3F2FD;        --color-work-text: #64B5F6;      /* 工作 - 浅蓝 */
  --color-meeting: #F3E5F5;     --color-meeting-text: #BA68C8;   /* 会议 - 浅紫 */
  --color-life: #FFF8E1;        --color-life-text: #FFB74D;      /* 生活 - 浅黄 */
  --color-shopping: #FCE4EC;    --color-shopping-text: #F48FB1;  /* 购物 - 浅粉 */
  --color-longterm: #E0F7FA;    --color-longterm-text: #4DD0E1;  /* 长期计划 - 浅青 */
  --color-other: #EFEBE9;       --color-other-text: #A1887F;     /* 其他 - 浅棕 */
}
```

**圆角与阴影体系**：

```css
:root {
  /* 圆角 —— 从 12px 到胶囊形的渐进 */
  --radius-sm: 12px;
  --radius-md: 16px;
  --radius-lg: 20px;
  --radius-full: 9999px;        /* 胶囊形（药丸按钮/标签） */

  /* 阴影 —— 暖棕色调的柔和阴影 */
  --shadow-soft: 0 2px 12px rgba(139,109,70,0.06);
  --shadow-card: 0 4px 20px rgba(139,109,70,0.08);
  --shadow-elevated: 0 8px 40px rgba(139,109,70,0.12);
}
```

> **设计要点**：所有颜色、圆角、阴影通过 CSS 变量管理，一处修改全局生效。类别色采用"浅底深字"原则，7 种类别对应 7 组配色。阴影使用暖棕色调（`rgba(139,109,70,...)`），与整体暖色系协调。圆@最小 12px，配合充足留白营造柔和感。

### 4.7 提醒系统：双通道温馨提醒

每 30 秒检查一次任务提醒时间，匹配后触发应用内 Toast 和 Electron 原生通知。

```javascript
function checkReminders() {
  const now = new Date()
  const tasks = storage.getTasks()
  tasks.forEach(task => {
    if (task.reminderTime && !task.reminded && task.status !== 'completed') {
      const reminderTime = new Date(task.reminderTime)
      // 匹配当前 1 分钟内
      if (Math.abs(now - reminderTime) < 60 * 1000) {
        const message = randomReminderMessage(task.title)
        activeReminders.push({ task, message })
        // 触发 Electron 原生通知
        window.electronAPI?.showNotification('治愈计划提醒', message)
      }
    }
  })
}
```

> **设计要点**：提醒文案从 5 条温馨模板中随机选择，如"该做 xxx 啦，加油哦~"。同时触发应用内 Toast 和 Electron 原生系统通知（即使窗口最小化也能看到）。已提醒的任务标记 `reminded: true`，避免重复提醒。

## 五、数据持久化设计

所有数据（任务、便利贴、设置、桌宠状态）通过 localStorage 持久化，应用关闭后数据不丢失。

```javascript
const DB_KEY = 'healing_planner_db'

function getDB() {
  const raw = localStorage.getItem(DB_KEY)
  if (!raw) {
    const initial = {
      tasks: [],
      stickies: [],
      settings: {
        petName: '小团子', petType: 'dumpling',
        reminderEnabled: true, reminderSound: true,
        apiProvider: 'mock', apiEndpoint: '', apiKey: '', apiModel: '',
        voiceMode: 'online', sttApiProvider: 'whisper',
        sttApiEndpoint: '', sttApiKey: '', sttApiModel: ''
      },
      petState: { mood: 'happy', lastMessage: '你好呀，今天也要加油哦~', totalCompleted: 0 }
    }
    localStorage.setItem(DB_KEY, JSON.stringify(initial))
    return initial
  }
  return JSON.parse(raw)
}

export const storage = {
  getTasks() { return getDB().tasks },
  addTask(task) { const db = getDB(); db.tasks.push(task); saveDB(db) },
  updateTask(id, updates) { /* 查找 → 合并 → 保存 */ },
  deleteTask(id) { /* 过滤 → 保存 */ },
  // 便利贴、设置、桌宠同理
}
```

> **设计要点**：单一 `DB_KEY` 管理所有数据，结构清晰。首次访问自动初始化默认数据。所有 Store 通过 `storage` 对象读写，保证数据一致性。后续可替换为 SQLite 或远程 API，只需修改 `storage.js`。

## 六、构建与运行

### 6.1 环境要求

- Node.js >= 16
- npm >= 8
- 操作系统：Windows / macOS / Linux

### 6.2 安装与开发

```bash
# 克隆项目
git clone <仓库地址>
cd healing-planner

# 安装依赖（推荐使用镜像源加速）
npm config set registry https://registry.npmmirror.com
npm config set electron_mirror https://npmmirror.com/mirrors/electron/
npm install

# 浏览器开发模式
npm run dev
# 访问 http://localhost:5173

# Electron 桌面应用开发模式
npm run electron:dev

# 构建 Windows 安装包
npm run electron:build
```

构建产物在 `dist-electron/` 目录下，生成 NSIS 安装包（`.exe`），支持自定义安装目录、桌面快捷方式、开始菜单快捷方式。

### 6.3 在线体验

应用已部署到 GitHub Pages，可直接在线体验（Web 版本，Electron 桌面特性需下载安装包体验）：

**在线 Demo**：[待部署后补充链接]

## 七、AI 测试指南

应用内置了**模拟测试模式**，无需任何真实 API 即可体验 AI 智能解析：

1. 进入 **设置** 页面
2. 在「AI 智能解析」区域，点击 **模拟测试** 卡片
3. 点击「测试连接」，提示模拟模式已启用
4. 进入 **智能输入** 页面，输入：`明天上午十点交软件工程报告，下午三点开组会，周五前把PPT做完，晚上背英语单词`
5. 点击「AI 解析」按钮，等待约 1 秒，即可看到解析结果

如需接入真实 API，选择对应服务商，填入 API Key 和模型名称即可。

## 八、功能效果展示

### 首页仪表盘

- 顶部问候语（根据时段显示"早上好/上午好/中午好/下午好/晚上好/夜深了"）
- 日期 + 励志文案（"每一步都算数"、"慢慢来，比较快"等轮换）
- 进度卡片：SVG 环形进度 + 进度条 + 完成数/总数 + 鼓励文案
- 今日任务列表（按优先级排序）
- 即将到来任务（未来 3 天内，最多 5 条）

### 智能输入页

- 文本输入区：大文本框，支持 `Ctrl+Enter` 快捷解析
- 语音输入：在线识别 / 录音转写双模式切换
- AI 解析：支持模拟解析和真实 API 调用（6 家提供商 + 自定义）
- 解析结果展示：可编辑的任务卡片
- 输入小贴士

### 任务列表页

- 时间筛选标签：今日 / 本周 / 全部
- 下拉筛选：类别 / 优先级 / 状态
- 任务卡片：左侧优先级色条，圆形完成按钮，类别/优先级标签
- 编辑弹窗：毛玻璃遮罩

### 便利贴页

- 彩色便利贴卡片：7 种类别对应 7 种背景色
- 悬停微旋转 + 上浮效果
- 一键完成 / 删除
- 完成便利贴时同步完成原任务

### 桌宠

- 位置：右下角，可拖动到任意位置
- 点击互动：眨眼 + 飘心 + 随机文案（13 条）
- 4 种心情：开心（绿色）/ 兴奋（橙色，弹跳）/ 担心（浅蓝）/ 难过（浅蓝）
- 进度环：SVG 显示今日完成百分比

### 设置页

- AI 智能解析配置：7 个提供商选择，API 地址/Key/模型配置，连接测试
- 语音转写 API 配置：5 个提供商选择
- 桌宠设置：名称 / 类型（小团子/小猫/小熊/云朵）
- 提醒设置：启用提醒 / 提醒音效开关
- 桌面应用设置：开机自启动（仅 Electron 环境显示）
- 数据管理：导出 JSON / 导入 JSON / 清除数据

## 九、扩展方向

| 方向 | 说明 | 改动点 |
|------|------|--------|
| 接入大模型 | 替换 `smartParse()` 为大模型 API 调用 | `taskParser.js` |
| 语音识别升级 | 替换 Web Speech API 为专业语音服务 | `voiceInput.js` |
| 桌面悬浮便利贴 | Electron BrowserWindow 实现独立小窗口 | `electron/main.js` |
| 云同步 | 数据存储替换为远程 API | `storage.js` |
| SQLite 存储 | 替换 localStorage 为 better-sqlite3 | `storage.js` |
| 复杂桌宠动画 | Lottie / Spine 动画替换静态 SVG | `DesktopPet.vue` |
| 主题切换 | 支持多种治愈色系主题 | `main.css` + `settingsStore.js` |
| 全局快捷键 | 快捷键呼出窗口/快速添加 | `electron/main.js` + `preload.js` |

## 十、总结

本案例完整演示了如何使用 **Electron + Vue 3 + Pinia** 技术栈构建一个 PC 端治愈系智能计划管理桌面应用。核心亮点包括：

1. **Electron 桌面应用壳**：无边框窗口 + 系统托盘 + 窗口状态记忆 + 开机自启 + NSIS 安装包
2. **自然语言解析引擎**：25 种时间模式 + 7 种类别检测 + 智能提醒时间计算，代码结构支持无缝切换到大模型
3. **语音输入双模式**：在线识别（Web Speech API）+ 录音转写（MediaRecorder + STT API），适应不同网络环境
4. **AI Mock 模式**：内置模拟解析，无需 API 即可测试完整流程，支持 6 家提供商 + 自定义
5. **可拖动桌宠**：拖动 + 点击互动 + 4 种心情 + 进度环 + 13 条治愈文案
6. **治愈风格 UI**：CSS 变量驱动的暖色设计系统，奶油白/浅绿/浅蓝/浅粉配色，大圆角，柔和阴影
7. **提醒系统**：应用内 Toast + Electron 原生通知双通道，温馨文案
8. **模块化架构**：Services / Stores / Components 三层分离，Electron 主进程通过 contextBridge 安全通信，易于扩展

项目代码已模块化组织，各功能模块独立可替换，方便后续接入大模型接口、语音识别服务和系统通知能力。

---

**相关链接**：

- 在线 Demo：[待部署后补充链接]
- 技术栈：Electron 33 + Vue 3.5 + Vite 6 + Pinia 2.2
- License：MIT

> 欢迎交流讨论，如果你也喜欢治愈系设计或者在做类似的效率工具，欢迎留言分享你的想法~